a. ผลลัพธ์ที่ได้ somrthing went wrong เพราะ a += 0 ได้ค่าเท่ากับ 1 และ a + c คือ 1 + '' ได้เท่ากับ b = 1'1' ซึ่งเข้าสู่ if else ทำให้ a ไม่เท่ากับ b
b. ผลลัพธ์เหมือนกัน เพราะ var square = function(n) คือนำฟังก์ชั่นเก็บไปในตัวแปร ส่วน function square(n) คือ การประกาศฟังก์ชันเพื่อรับค่า n
